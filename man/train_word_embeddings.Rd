% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_embeddings.R
\name{train_word_embeddings}
\alias{train_word_embeddings}
\title{Train word embeddings from a corpus}
\usage{
train_word_embeddings(
  corpus = corpus_ITA,
  model_name = "model1",
  country = "Italy",
  speaker_party = "Speaker_party",
  year_var = "year",
  num_bootstraps = 100,
  window_size = 10,
  drop_parties = c("RE"),
  min_docs = 50,
  output_dir = "./models_wordembeddings/"
)
}
\arguments{
\item{corpus}{A tokenized quanteda corpus. Defaults to \code{corpus_ITA}.}

\item{model_name}{A custom name to label your saved models.}

\item{country}{A string indicating the country (used in folder structure).}

\item{speaker_party}{The docvar indicating actor name.}

\item{year_var}{The name of the docvar containing the year (as an integer or
coercible to integer). Defaults to \code{"year"}.}

\item{num_bootstraps}{Number of bootstrap samples per year.}

\item{window_size}{The size of the context window.}

\item{drop_parties}{A vector of actors to exclude.}

\item{min_docs}{Minimum number of documents required per year.}

\item{output_dir}{Path to the directory for storing models (relative to working directory).}
}
\value{
Saves RDS files with word embeddings; returns nothing.
}
\description{
This function trains GloVe-based word embeddings for individual speakers
using a bootstrapping procedure on a \code{quanteda::tokens} object.
}
\details{
If \code{corpus} is \code{NULL}, the function uses a built-in Italian
tokens corpus shipped with the package. For other countries, users
must supply their own tokens object.
}
\examples{
\dontrun{
train_word_embeddings(corpus = your_tokens_object)
}
}
